# Web
fastapi>=0.109.0
uvicorn[standard]>=0.27.0

# 配置（仅 YAML，无 pydantic-settings）
pyyaml>=6.0.2

# 数据库（PostgreSQL + pgvector）
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0
pgvector>=0.3.0

# 认证与安全（直接使用 bcrypt，避免与 passlib 版本冲突）
bcrypt>=4.0.0
pyjwt>=2.8.0
email-validator>=2.0.0

# 工作流 / LangChain 组件（用户输入 -> 大模型 等）
langchain-core>=0.3.0
langchain-openai>=0.2.0

# 测试
pytest>=8.0.0
httpx>=0.27.0
